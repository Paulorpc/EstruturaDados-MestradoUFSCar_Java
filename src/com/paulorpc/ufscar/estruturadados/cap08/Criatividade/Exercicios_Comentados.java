package com.paulorpc.ufscar.estruturadados.cap08.Criatividade;


public class Exercicios_Comentados {
	
	/*
	 * 8.4
	 * 
	 * Crio uma fila (ordenada ou não ordenada) e uma variável incremental que
	 * será a chave do elemento. O método remove, será o remove retorna o maior
	 * elemento, assim para cada inserção faz-se um incremento na variavel chave
	 * 
	 * chave = 0;
	 * insert(chave++, E e)
	 * remove() //remove max 
	 * 
	 */

}

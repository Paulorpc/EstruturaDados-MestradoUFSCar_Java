package com.paulorpc.ufscar.estruturadados.cap05.Criatividade.Ex2;


/**
 * @author paulo
 * @date 12 de mai de 2017
 *
 */
public class Main {
	
	/*
	 
	Describe how to implement the stack ADT using a single queue as an instance
	variable, and only constant additional local memory within the method bodies.
	What is the running time of the push(), pop(), and top() methods for your design?
	
	metodo push(E el) {
	
		No n = novo No(el, Top)
		top = n;
		size++;
	
	} => O(1)
	
	metodo E pop() {
	
		E tmp = top.getElement();
		top = top.getNext();
		size--;
		retorno tmp;
	
	} => O(1)
	
	metodo top() {
	
		return top.getElement();
	
	} => O(1)
	 
	 
	 */

}
